<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    {% render "head", metadata: site.metadata, title: data.title %}
  </head>
  <body class="{{ data.layout }} {{ data.page_class }}">
    <header>
    </header>

    <nav>
      <ul>
        <li><a href="{{ '/' | relative_url }}">Game List</a></li>
        <li><a href="https://buttonshygames.com/">Button Shy Games</a></li>
      </ul>
    </nav>

    <main>
      <article>
        <section>
          {{ content }}
        </section>
      </article>
    </main>

    <footer>
      Contact me on Button Shy's discord as <strong>@jamie_ca</strong>
    </footer>
  </body>
</html>
