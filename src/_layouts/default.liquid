<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    {% render "head", metadata: site.metadata, title: data.title %}
  </head>
  <body class="{{ data.layout }} {{ data.page_class }}">
    <header>
    </header>

    <nav>
      <ul>
        <li><a href="{{ '/' | relative_url }}">Home</a></li>
      </ul>
    </nav>

    <main>
      <article>
        <section>
          {{ content }}
        </section>
      </article>
    </main>

    <footer>
      Contact me on the <a href="https://discord.gg/6cFmdG2jEj">Button Shy discord</a> as <strong>@jamie_ca</strong>
    </footer>
  </body>
</html>
